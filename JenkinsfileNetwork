pipeline {
    agent any

    stages {
        stage('Ansible Lint') {
            steps {
                sh 'ansible-lint ansible-gql/old\\ playbooks/bgp.yml'
                //sh 'ansible-lint ansible-gql/init-routing-bgp.yml'
            }
        }
    }
}
